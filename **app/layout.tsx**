import { Metadata } from "next";
import "./globals.css";
export const metadata: Metadata = {
 title: "Bank RPN Indonesia",
 description: "Aplikasi perbankan untuk Pi Network",
};
Export default function RootLayout({
 children,
}:Readonly<{
 children: React.ReactNode;
}<){
 return (
  <html lang="id">
  </html>
);
}
---
